on: push
name: Deploy website on push
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v2

      - name: Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.upti_ftp_server }}
          protocol: ftps
          port: 22
          username: ${{ secrets.upti_ftp_username }}
          password: ${{ secrets.upti_ftp_password }}
